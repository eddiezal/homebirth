import { ResourcesContent } from "./_components/ResourcesContent";

export const metadata = {
  title: "Resources — Homebirth.com",
  description: "Everything you need to make a confident decision about homebirth. Honest answers to the questions most people have before they start.",
};

export default function ResourcesPage() {
  return <ResourcesContent />;
}