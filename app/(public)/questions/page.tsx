import { QuestionsContent } from "./_components/QuestionsContent";

export const metadata = {
  title: "Question Library — Homebirth.com",
  description: "30+ questions to ask when evaluating a midwife or birth center. Organized by category — safety, trust, budget, care, and more.",
};

export default function QuestionsPage() {
  return <QuestionsContent />;
}